# Port de l'application Spring Boot
server.port=8080

# Configuration de la base de donnees PostgreSQL (Docker)
spring.datasource.url=jdbc:postgresql://localhost:5432/gestionevenements
spring.datasource.username=tech57_user
spring.datasource.password=tech57_password
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuration JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true